module.exports = {
    purge: {},
    darkMode: false,
    theme: {
        extend: {},
    },
    variants: {
        extend: {
            padding: ['hover'],
            borderRadius: ['hover']
        },
    },
    plugins: [],
}
